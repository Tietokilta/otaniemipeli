{
  "name": "otaniemipeli",
  "version": "0.5.0",
  "description": "",
  "private": true,
  "packageManager": "pnpm@10.16.1",
  "scripts": {
    "dev": "pnpm --filter frontend dev",
    "build": "pnpm --filter frontend build",
    "start": "pnpm --filter frontend start",
    "lint": "pnpm --filter frontend lint",
    "lint:fix": "pnpm --filter frontend lint-fix",
    "format": "prettier --write . && cargo fmt --manifest-path packages/backend/Cargo.toml --all --",
    "format:check": "prettier --check .",
    "typecheck": "pnpm --filter frontend typecheck",
    "clean": "rm -rf .next node_modules && pnpm install && pnpm --filter frontend clean",
    "backend": "docker compose up --build",
    "backend:build": "cargo build --release --manifest-path=packages/backend/Cargo.toml",
    "production:build": "docker compose -f docker-compose.prod.yml up --build",
    "typegen": "python3 scripts/generateTStypes.py",
    "turn:gen": "python3 scripts/simulateTurns.py"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Tietokilta/otaniemipeli.git"
  },
  "devDependencies": {
    "prettier": "^3.7.4"
  }
}
