# ── Ports ──
POSTGRES_PORT=5432
PUBLIC_PG_PORT=5433
BACKEND_PORT=2568
FRONTEND_PORT=3000

# ── Postgres ──
POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_DB=
POSTGRES_HOST=kanta
POSTGRES_URL=

# ── Flyway (must mirror Postgres credentials) ──
FLYWAY_USER=
FLYWAY_PASSWORD=
FLYWAY_URL=
FLYWAY_LOCATIONS=filesystem:./sql

# ── Backend ──
SALT=
RUST_BACKTRACE=full
FRONTEND_URL=http://localhost:3000

# ── Frontend (client-side, baked into JS bundle at build time) ──
NEXT_PUBLIC_API_URL=http://localhost:2568/api/v1
NEXT_PUBLIC_API_BASE_URL=http://localhost:2568
